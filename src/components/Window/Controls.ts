import Element from '$lib/Element';

function Controls() {

    return (
        new Element<HTMLDivElement>({
            tagName: 'div',
            props: {

            },
        }).dom
    );
}

export default Controls;
