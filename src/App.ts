import MenuBar from '$components/MenuBar';
import Dock from './components/Dock';

function App() {

    return (
        [
            MenuBar(),
            Dock()
        ]
    );
}

export default App;
